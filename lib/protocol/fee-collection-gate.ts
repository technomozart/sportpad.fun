/** Pump fee indexing observes distributions; it does not collect them. A
 * signed, permissionless, recoverable collector must be verified separately
 * before public launches promise unattended rewards. */
export const AUTOMATED_PUMP_FEE_COLLECTION_VERIFIED = false;
